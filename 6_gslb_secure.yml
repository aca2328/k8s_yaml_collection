
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: secure1
spec:
  tls:
  - hosts:
    - sslapp.ako.avilab.me
    secretName: certif1
  rules:
    - host: sslapp.ako.avilab.me
      http:
        paths:
        - path: /
          backend:
            serviceName: s1
            servicePort: 80
