apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: secure2
spec:
  tls:
  - hosts:
    - secure2.ako.avilab.me
    secretName: certif1
  rules:
    - host: secure2.ako.avilab.me
      http:
        paths:
        - path: /
          backend:
            serviceName: s1
            servicePort: 80
