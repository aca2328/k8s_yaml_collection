apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  labels:
    app: gslb
  name: insecure
spec:
  rules:
  - host: app1.ako.avilab.me
    http:
      paths:
      - backend:
          serviceName: green
          servicePort: 80
