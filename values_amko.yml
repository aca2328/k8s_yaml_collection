# Default values for amko.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: 10.xx.xx.xx:5443/amko
  pullPolicy: IfNotPresent

configs:
  gslbLeaderHost: "10.206.112.46"
  controllerVersion: "18.2.8"
  gslbLeaderSecret: "gslb-avi-secret"
  memberClusters:
    - clusterContext: "cluster1-admin"
    - clusterContext: "cluster2-admin"
  refreshInterval: 1800
  gslbLeaderCredentials:
    username: "admin"
    password: "xxxxx"
  domainNames:
    - ako.avilab.me
  clusterMembers:
    secret: gslb-config-secret
    key: gslb-members

gdpConfig:
  # appSelector takes the form of:
  # appSelector:
  #   label:
  #     app: gslb   <example label key-value for an ingress/service type LB>
  appSelector:

  # namespaceSelector takes the form of:
  # namespaceSelector:
  #   label:
  #     ns: gslb   <example label key-value for namespace>
  namespaceSelector:

  # list of all clusters that the GDP object will be applied to, can take any/all values
  # from .configs.memberClusters
  matchClusters:
    - "cluster1-admin"
    - "cluster2-admin"

  # list of all clusters and their traffic weights, if unspecified, default weights will be
  # given
  trafficSplit:
    - cluster: "cluster1-admin"
      weight: 5
    - cluster: "cluster2-admin"
      weight: 5

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

service:
  type: ClusterIP
  port: 80

persistentVolumeClaim: ""
mountPath: "/log"
logFile: "amko.log"
